//EL OBJETO MATH

let resultado;

resultado= Math.PI;
resultado=Math.round(2.5);
resultado=Math.ceil(2.1); //redoendea hacia arriab
resultado=Math.floor(2.1);//floor redodea hacia abajo
resultado=Math.sqrt(144);//saca la raiz cuadrada
resultado=Math.abs(-200);//convierte em negativo el numero
resultado=Math.min(2, 35,9.5);//toma el minimo
resultado=Math.max(2, 35,9.5)// toma el mayor
resultado=Math.random();
resultado=Math.floor(Math.random()*30); //mexcalndo unha funcion con otra funcion


console.log(resultado);